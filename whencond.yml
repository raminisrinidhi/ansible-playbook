---
- hosts: webservers
  user: ansible
  become: true
  connection: ssh
  tasks:
    - name: gather installed package facts
      action: package_facts manager=auto
    - name: installing git package
      action: yum pkg=git state=present
      when: "'git' not in ansible_facts.packages"
